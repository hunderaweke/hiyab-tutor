# =============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env on your VPS and configure with production values
# DO NOT commit the actual .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# JWT Configuration
# -----------------------------------------------------------------------------
# Generate a strong random secret (min 32 characters)
# Command: openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET_MINIMUM_32_CHARACTERS

# -----------------------------------------------------------------------------
# Admin Credentials (Initial Admin User)
# -----------------------------------------------------------------------------
# These credentials will be used to create the default admin account
# Change these immediately after first login!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ChangeThisSecurePassword123!

# -----------------------------------------------------------------------------
# Application URLs
# -----------------------------------------------------------------------------
# Your VPS public IP or domain name
# Example: http://85.31.239.243 or https://yourdomain.com
WEB_APP_URL=http://YOUR_VPS_IP_OR_DOMAIN

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Backend API server port (internal container port)
SERVER_PORT=8080

# -----------------------------------------------------------------------------
# PostgreSQL Database Configuration
# -----------------------------------------------------------------------------
# Database hostname (use 'postgres' for Docker network)
BLUEPRINT_DB_HOST=postgres
BLUEPRINT_DB_PORT=5432
BLUEPRINT_DB_DATABASE=hiyab_tutor
BLUEPRINT_DB_USERNAME=postgres
BLUEPRINT_DB_PASSWORD=ChangeThisSecureDatabasePassword123!
BLUEPRINT_DB_SCHEMA=public

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------
# Public website port (host machine)
FRONTEND_PORT=80

# -----------------------------------------------------------------------------
# Dashboard Configuration
# -----------------------------------------------------------------------------
# Admin dashboard port (host machine)
DASHBOARD_PORT=3000

# =============================================================================
# PRODUCTION CHECKLIST
# =============================================================================
# [ ] Generate strong JWT_SECRET using: openssl rand -base64 32
# [ ] Set secure ADMIN_PASSWORD (min 12 chars, mixed case, numbers, symbols)
# [ ] Set secure BLUEPRINT_DB_PASSWORD
# [ ] Update WEB_APP_URL with your actual domain or IP
# [ ] Review all values before deployment
# [ ] Never commit this file to git (it's in .gitignore)
# [ ] Keep a secure backup of this file
# =============================================================================
